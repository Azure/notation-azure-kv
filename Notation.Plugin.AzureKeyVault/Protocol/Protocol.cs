namespace Notation.Plugin.Protocol
{
    // Notation plugin Protocol class defines the required constants;
    public static class Protocol
    {
        public const string ContractVersion = "1.0";
    }
}